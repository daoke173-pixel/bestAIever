PARTS = [
("omega_block", "Ω-Block: The Core Residual Unit", "The fundamental repeated block combining attention, SSM, MoE and latent program slots.", ["block topology with parallel attention/SSM branches and learned mixing gates", "residual scaling and depth-stable initialisation derivation", "per-branch precision and determinism declarations", "ablation harness measuring each branch's contribution"]),
("hybrid_mixer", "Attention/SSM Hybrid Mixing Policy", "Decides, per layer and per token, how much exact attention versus linear state is used.", ["layer-interleaving schedule search and learned per-token gating", "quality/compute tradeoff curves at multiple ratios", "long-range retrieval probe accuracy versus pure attention", "stability analysis of the gate under distribution shift"]),
("attention_variants", "Attention Variant Zoo & Selection", "Every attention form the model can use, with a principled selection rule.", ["MHA/GQA/MQA/MLA-style latent compression with head-budget analysis", "sliding-window, dilated, global-token and retrieval-augmented forms", "per-layer variant assignment optimiser", "measured quality/cost frontier per variant"]),
("kv_compression_model", "Learned KV Compression", "Shrinks the KV cache by learning what is worth remembering.", ["low-rank latent KV projection with reconstruction objective", "importance-predicting head for eviction decisions", "quality-preservation validation on long-context tasks", "measured memory reduction at matched accuracy"]),
("state_space_layer", "Selective State-Space Layer", "The O(N) memory backbone for extreme context.", ["input-dependent A/B/C parameterisation with stability constraints", "state initialisation and reset semantics across documents", "gradient-flow analysis over very long sequences", "recall accuracy on needle-in-haystack probes"]),
("recurrent_memory_layer", "Recurrent Compressive Memory Layer", "Carries information across millions of tokens with bounded state.", ["compressive memory update rule with write/erase gating", "capacity analysis and interference measurement", "read-out attention over memory slots", "multi-document recall benchmarks"]),
("latent_program_slots", "Latent Program Slot Mechanism", "Lets the network allocate reusable internal subroutines rather than re-deriving them.", ["slot bank with addressing, binding and reuse counting", "program-induction objective and slot specialisation analysis", "interpretability probes naming what each slot computes", "measured gain on compositional generalisation tasks"]),
("adaptive_depth", "Adaptive Computation Depth", "Easy tokens exit early; hard tokens recurse deeper (part of speed source S2).", ["per-token halting head with calibrated confidence", "layer-recurrence for hard tokens with shared weights", "compute-vs-accuracy curves and budget-constrained mode", "worst-case latency bound proof"]),
("normalisation_design", "Normalisation & Residual Scaling Design", "The choices that make a very deep, very sparse model trainable and stable.", ["pre/post/sandwich norm comparison with gradient-norm evidence", "QK-norm and logit-cap for attention stability", "depth-dependent residual scaling derivation", "training-stability evidence at extreme depth"]),
("activation_design", "Activation & Gating Function Design", "Activation choices justified by measurement, not tradition.", ["gated-linear family comparison at matched parameter and FLOP budgets", "numerical range analysis for low-precision safety", "expressivity arguments and empirical ablations", "kernel-fusion friendliness assessment"]),
("embedding_design", "Tokenisation-Aware Embedding & Unembedding", "Input/output representations, including tied and factorised forms.", ["factorised embedding with rank selection", "tied-weight analysis and separate-head comparison", "vocabulary-frequency-aware initialisation", "measured perplexity and downstream effect"]),
("tokeniser_omega", "Ω-Tokeniser: Byte-Level Adaptive Segmentation", "Fixes the tokenizer tax that inflates Opus-class token counts by up to 35%.", ["byte-level BPE with learned merge policy and code/math-aware pre-tokenisation", "multilingual fairness measurement of tokens-per-character", "adaptive patching that lets frequent structures cost one token", "measured token-count reduction versus the Opus 4.7+ tokenizer"]),
("byte_latent_patching", "Byte-Latent Patch Encoder", "Tokeniser-free path: entropy-driven patching straight from bytes.", ["entropy-model-driven patch boundary prediction", "patch encoder/decoder with variable compression rate", "robustness to typos, code, binary and rare scripts", "quality/compute comparison against the tokenised path"]),
("positional_design", "Positional Representation Strategy", "Position handling that extrapolates far past training length.", ["RoPE-family scaling analysis with theoretical extrapolation bounds", "learned-relative and no-position ablations", "position interpolation policy per attention variant", "length-generalisation curves to 10x training length"]),
("long_context_arch", "Ultra-Long Context Architecture", "The design that makes an effective 1G-token context feasible.", ["hierarchical context: exact recent window, compressed mid, retrieved far", "cross-level consistency and information-routing analysis", "cost model per context tier", "recall and reasoning benchmarks at extreme lengths"]),
("multi_token_prediction", "Multi-Token Prediction Heads", "Predicts several future tokens per step: better representations and faster decode.", ["head architecture with shared trunk and per-offset calibration", "training objective weighting across offsets", "integration with the speculative cascade as a free draft source", "measured acceptance-rate contribution to S1"]),
("draft_model_arch", "Ω-Draft Model Family Architecture", "The 1.5B/7B/70B drafters that power the 8-stage cascade.", ["capacity ladder design with shared vocabulary and KV layout", "distillation-friendly architecture choices for high acceptance", "per-stage acceptance-rate targets and measurement", "memory footprint budget for co-residency with the core"]),
("verifier_head", "Internal Verifier & Confidence Heads", "The model's own critic: predicts correctness before the answer ships.", ["per-claim and per-step correctness heads with calibration", "abstention/escalation policy driven by the verifier", "selective-prediction risk-coverage curves", "measured reduction in confident errors versus baseline"]),
("uncertainty_calibration", "Uncertainty Representation & Calibration", "Numbers you can act on: calibrated probabilities everywhere.", ["temperature/vector scaling and conformal prediction integration", "epistemic-vs-aleatoric decomposition", "calibration metrics (ECE, ACE) with target thresholds", "decision-theoretic use in routing and escalation"]),
("world_model_core", "Internal World Model & State Tracking", "Explicit, updatable beliefs about entities, quantities and constraints.", ["entity/relation state representation with typed slots", "update rules from text, tools and observations with conflict handling", "consistency checking against retrieved evidence", "state-tracking benchmark accuracy on long narratives"]),
("causal_model", "Causal Representation & Intervention Reasoning", "Distinguishes correlation from causation in the model's own reasoning.", ["structural causal model representation and do-calculus evaluation", "confounder identification from context", "counterfactual query answering with assumptions made explicit", "benchmark on causal-inference question suites"]),
("symbolic_bridge", "Neural/Symbolic Interface Layer", "Lets the network hand a subproblem to an exact solver and use the result.", ["differentiable-to-symbolic translation with type checking", "solver result integration and belief update", "failure handling when symbolic translation is impossible", "measured accuracy gain on math and constraint tasks"]),
("program_induction_arch", "Program Induction & Abstraction Learning", "Learns reusable abstractions from few examples: the ARC-AGI-3 engine.", ["DSL design for grid/graph/sequence transformations", "library learning with compression-based abstraction discovery", "neural-guided enumerative synthesis over the DSL", "target: 88% on ARC-AGI-3 versus Opus 5's 30.2%"]),
("meta_learning_arch", "In-Context Meta-Learning Architecture", "Learns new tasks within a single conversation and keeps the skill.", ["fast-weight/adapter-in-context mechanism with bounded capacity", "task-inference head identifying the current task family", "catastrophic-interference measurement and mitigation", "few-shot learning curves versus baseline in-context learning"]),
("continual_learning", "Continual Learning Without Forgetting", "Absorbs new knowledge after deployment, safely and reversibly.", ["parameter-isolation and replay strategies with forgetting bounds", "knowledge-edit application with locality verification", "rollback and provenance for every learned change", "forgetting/plasticity tradeoff measurement"]),
("knowledge_editing", "Targeted Knowledge Editing", "Change one fact without breaking a thousand others.", ["locate-then-edit method with causal tracing", "ripple-effect evaluation across paraphrases and implications", "batch-edit interference analysis", "edit success/locality/generalisation metrics"]),
("model_merging", "Model Merging & Capability Composition", "Combines specialists into one model without averaging away skill.", ["task-vector arithmetic with interference resolution", "permutation alignment before merging", "merge-quality prediction and layer-wise weighting", "measured retention of each specialist's capability"]),
("distillation_arch", "Distillation Architecture & Objectives", "Transfers Ω-core quality into the fast paths (enables S1 and S4).", ["logit/feature/attention-map distillation with matched tokenisation", "on-policy distillation from the core's own search traces", "capacity-gap handling with intermediate teachers", "quality-retention measurement per size tier"]),
("weight_sharing", "Weight Sharing & Parameter Efficiency", "More capability per byte of memory.", ["cross-layer sharing schemes with per-layer modulation", "low-rank and structured factorisation with rank selection", "hypernetwork-generated weights for rare paths", "quality-per-parameter frontier measurement"]),
("init_scaling_laws", "Initialisation & Scaling Law Framework", "Predicts, before training, what a configuration will achieve.", ["muP-style parameterisation for hyperparameter transfer across scale", "compute-optimal allocation including sparsity and data repetition", "loss-prediction fits with confidence intervals", "prediction accuracy validated on held-out scales"]),
("architecture_search", "Architecture Search & Ablation Engine", "Systematically finds the best block design under real constraints.", ["search space definition with hardware-aware constraints", "proxy tasks and early-stopping predictors for cheap evaluation", "multi-objective selection (quality, latency, memory, energy)", "discovered-versus-baseline architecture comparison"]),
("depth_width_tradeoff", "Depth/Width/Sparsity Allocation", "Where to spend the parameter budget, decided by measurement.", ["allocation sweep methodology at matched compute", "reasoning-depth versus knowledge-capacity analysis", "latency implications of depth under the cascade", "recommended configuration with supporting evidence"]),
("residual_stream_design", "Residual Stream Capacity & Interference", "Prevents features from colliding in a heavily shared representation.", ["stream-width analysis and superposition measurement", "feature-interference detection and mitigation via subspace allocation", "read/write pattern analysis per layer", "interpretability-driven design recommendations"]),
("logit_head_design", "Output Head, Logit Shaping & Constraints", "Correct, constrained, calibrated output distributions.", ["logit soft-capping and vocabulary masking mechanics", "grammar/schema-constrained decoding integration at the head", "temperature/penalty semantics defined exactly", "constraint-satisfaction guarantees with proofs"]),
("multimodal_fusion_arch", "Multimodal Fusion Architecture", "One backbone, many senses, no modality tax.", ["early/mid/late fusion comparison with cross-attention adapters", "modality-specific tokenisation into a shared latent space", "modality dropout and robustness to missing inputs", "cross-modal transfer measurement"]),
("action_head", "Action & Tool-Call Head", "Generates structured actions with the same rigor as text.", ["typed action schema decoding with validity guarantees", "argument grounding against live environment state", "action-confidence estimation feeding the safety gate", "action validity rate measurement on agent benchmarks"]),
("memory_attention_bridge", "Memory Retrieval Attention Bridge", "How the model reads its own long-term memory mid-generation.", ["retrieval-in-the-loop attention with query reformulation", "relevance scoring and evidence-attribution tracking", "latency budget for in-loop retrieval", "measured accuracy gain on knowledge-intensive tasks"]),
("speculative_arch_hooks", "Architecture Hooks for Speculative Decoding", "Makes the core cheap to verify in parallel (foundation of S1).", ["tree-attention-compatible KV layout and mask construction", "batched multi-branch verification in one forward pass", "acceptance-rate instrumentation per branch depth", "verified equivalence with sequential decoding"]),
("sparse_upcycling", "Sparse Upcycling & Expert Initialisation", "Turns dense checkpoints into sparse ones without quality loss.", ["expert initialisation from dense weights with diversity injection", "router warmup schedule preventing collapse", "quality trajectory versus training-from-scratch", "compute saved measurement"]),
("model_surgery", "Model Surgery: Pruning, Grafting, Extension", "Safely modifies a trained model's structure.", ["structured pruning with sensitivity-guided selection and healing", "layer/expert grafting between compatible checkpoints", "context-length and vocabulary extension procedures", "post-surgery quality verification protocol"]),
("numerical_arch_stability", "Architectural Numerical Stability", "Design-level defences against divergence and low-precision failure.", ["activation-range budgeting per layer with measured margins", "attention-logit and gradient-explosion prevention mechanisms", "low-precision-safe formulation of every block", "extreme-input and long-run stability tests"]),
("context_packing", "Context Composition & Packing Strategy", "What actually goes into the window, and in what order.", ["document packing with attention isolation and position resets", "priority-based context assembly under a token budget", "lost-in-the-middle mitigation via ordering and restatement", "measured accuracy versus naive concatenation"]),
("prompt_representation", "Internal Prompt & Role Representation", "Unambiguous instruction hierarchy encoded in the architecture.", ["role/channel embeddings for system, developer, user, tool and thought", "instruction-priority encoding resistant to injection", "hierarchy-violation detection head", "injection-resistance benchmark results"]),
("thought_representation", "Latent Thought Channel", "Separates deliberation from output, with controllable visibility.", ["dedicated thought channel with its own budget and decoding policy", "thought-to-answer distillation reducing visible token count", "leakage prevention between channels", "measured accuracy gain per thought token spent"]),
("expert_specialisation", "Expert Specialisation Analysis & Design", "Makes 512 experts genuinely different from each other.", ["specialisation metrics (domain, syntax, modality) with clustering", "diversity-promoting objectives and initialisation", "redundancy detection and expert retirement criteria", "specialisation report over real traffic"]),
("model_config_schema", "Model Configuration Schema & Validation", "One validated description of every model variant.", ["exhaustive config schema with constraint checking", "derived-quantity computation (parameters, FLOPs, memory, latency)", "invalid-configuration rejection with explanations", "config-to-artifact provenance binding"]),
("reference_forward", "Reference Forward Pass Implementation", "Slow, transparent, obviously-correct model execution as the oracle.", ["pure implementation with no fused kernels and explicit intermediates", "intermediate-value dump format for debugging", "bit-exactness policy versus optimised paths", "cross-checked against optimised execution on a fixed corpus"]),
("arch_ablation_suite", "Architecture Ablation & Evidence Suite", "Every architectural claim backed by a reproducible experiment.", ["ablation matrix over all major design choices", "matched-compute experimental protocol with seeds and variance", "statistical analysis with confidence intervals", "evidence report for each design decision"]),
("capacity_probes", "Capability Probes & Representation Diagnostics", "Measures what the network actually represents, not just its loss.", ["linear/nonlinear probing suite over layers and features", "circuit-level capability localisation", "emergence tracking across training checkpoints", "probe-to-benchmark correlation analysis"]),
("arch_spec_doc", "Architecture Specification Document Generator", "The authoritative, auto-generated description of the Ω-core.", ["spec extraction from config and code with consistency checking", "diagram and tensor-shape-table generation", "parameter/FLOP accounting per component", "spec-vs-implementation drift detection"]),
]
