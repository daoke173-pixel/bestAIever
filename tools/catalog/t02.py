PARTS = [
("gemm_fp8", "FP8 Tensor-Core GEMM Kernel", "The primary matmul: FP8 inputs, BF16/FP32 accumulate, >=88% of hardware peak.", ["warp-specialised producer/consumer pipeline with async copy and mbarrier staging", "tile-shape and split-K selection table per (M,N,K) regime", "per-tensor/per-channel/per-block scaling with overflow-free accumulation", "peak-percentage assertion in the microbench gate"]),
("gemm_int4", "INT4/INT8 Mixed-Precision GEMM", "Weight-only and full-integer matmul for the cheapest decode paths.", ["4-bit packed weight layout with group-wise zero points and scales", "dequant-fused-in-register inner loop avoiding memory round-trips", "accuracy guard comparing against BF16 reference within declared error bound", "throughput table feeding the compiler's precision-selection pass"]),
("gemm_grouped", "Grouped & Batched GEMM for MoE", "Many-small-matmul kernel that makes 512-expert routing cheap.", ["variable-shape group descriptors with a single kernel launch", "load balancing across SMs when expert loads are skewed", "gather/scatter fused into the epilogue to avoid separate permute passes", "worst-case-skew benchmark including 100:1 imbalance"]),
("gemm_sparse", "Structured-Sparse GEMM (2:4 and block)", "Exploits structured sparsity for a further FLOP reduction.", ["2:4 metadata layout and sparse tensor-core instruction scheduling", "block-sparse tiling with a compressed block index", "sparsity-pattern validator and fallback to dense", "measured speedup versus dense at matched accuracy"]),
("attn_flash_fwd", "Fused Flash Attention Forward", "Memory-optimal attention forward with online softmax.", ["tiled QK^T with running max/sum and no materialised score matrix", "causal, sliding-window, block-diagonal and arbitrary-mask fast paths", "head-dim specialisations (64/96/128/192/256) with register blocking", "numerical-stability proof and extreme-magnitude tests"]),
("attn_flash_bwd", "Fused Flash Attention Backward", "Training-side attention gradient with recomputation.", ["dQ/dK/dV computation with deterministic accumulation order", "recompute-vs-store policy chosen by memory budget", "atomic-free deterministic mode for reproducible training", "gradient correctness against a finite-difference reference"]),
("attn_paged_decode", "Paged KV Decode Attention", "The single hottest inference kernel: one query step against a paged KV cache.", ["page-table indirection with coalesced gather and vectorised loads", "split-KV parallel reduction for long contexts", "multi-query and grouped-query head sharing", "p99 latency budget assertion at 1M-token context"]),
("attn_linear", "Linear & Kernel-Feature Attention", "O(N) attention variants used for ultra-long context tiers.", ["chunked linear attention with state recurrence and stable normalisation", "random/deterministic feature maps with variance analysis", "hybrid switchover policy versus exact attention", "quality-parity evaluation on long-range retrieval probes"]),
("ssm_scan", "Selective State-Space Scan Kernel", "Hardware-efficient associative scan powering the SSM half of the backbone.", ["work-efficient Blelloch scan with chunked recurrence and state passing", "input-dependent gating with numerically stable log-space accumulation", "bidirectional and causal variants", "bit-exact determinism across chunk-size choices"]),
("conv_depthwise", "Depthwise & Short-Convolution Kernels", "Local mixing operators used by SSM blocks and perception front-ends.", ["fused depthwise conv with causal padding and register reuse", "FFT and Winograd paths with crossover thresholds", "1D/2D/3D variants sharing one code path", "boundary-condition exhaustive tests"]),
("softmax_norm", "Softmax, LogSumExp & Normalisation Kernels", "Fused reductions with guaranteed stability and determinism.", ["online single-pass softmax with max subtraction", "RMSNorm/LayerNorm/QK-Norm fused with residual add and cast", "welford-based variance for long rows", "extreme-input stability suite (1e38, -1e38, all-equal, single-spike)"]),
("elementwise_fusion", "Elementwise Fusion Engine", "Runtime-fusible elementwise/activation chains with one memory pass.", ["expression-template fusion producing a single loop", "activation library: gelu/silu/swiglu/geglu with exact and approx modes", "in-place safety analysis and aliasing rules", "bandwidth-bound efficiency measurement versus theoretical peak"]),
("reduction_kernels", "Deterministic Reduction & Scan Library", "All-reduce-style local reductions with fixed pairwise order.", ["tree reduction with fixed topology independent of thread count", "Kahan/Neumaier compensated summation option", "segmented reductions for ragged batches", "batch-invariance verification driver"]),
("topk_sort", "Top-K, Sort & Sampling Kernels", "Selection primitives for routing, beam search and token sampling.", ["radix select with bitonic refinement for small K", "fused top-k + softmax + renormalise for sampling", "stable ordering under ties for determinism", "distribution-fidelity tests for nucleus/typical/min-p sampling"]),
("quantize_kernels", "Quantisation & Dequantisation Kernels", "Fast, accurate conversion between all supported precisions.", ["per-tensor/channel/group/block scale computation in one pass", "stochastic rounding and error-feedback variants", "outlier-aware mixed decomposition for hard layers", "round-trip error bounds per format with measured tables"]),
("kv_cache_kernels", "KV Cache Compression Kernels", "Quantised, evicted and delta-encoded KV storage operators.", ["FP8/INT4 KV quantise-store and dequantise-load fused into attention", "importance-scored eviction and compaction in place", "delta encoding for repeated prefixes", "quality impact measurement at each compression level"]),
("rope_embed", "Positional Encoding Kernels", "RoPE and successors, including extrapolation-safe variants.", ["fused RoPE apply with precomputed tables and cache-friendly access", "NTK/YaRN-style scaling for context extension", "position-id gather for packed/ragged batches", "extrapolation quality probes far beyond training length"]),
("gather_scatter", "Gather / Scatter / Permute Kernels", "Data-movement primitives that dominate MoE and retrieval paths.", ["vectorised gather with index bounds checking hoisted out of the loop", "conflict-free scatter with deterministic ordering", "fused permute+cast+quantise epilogues", "index-pattern benchmarks including worst-case random access"]),
("transpose_layout", "Layout Transform & Transpose Kernels", "Fast relayout between all tensor formats without extra passes.", ["tiled shared-memory transpose with bank-conflict-free padding", "fused transpose into GEMM prologues", "NHWC/NCHW/blocked-format conversion matrix", "bandwidth efficiency gate"]),
("rng_kernels", "Device-Side RNG Kernels", "Counter-based parallel RNG matching the CPU split_seed exactly.", ["ChaCha/Philox counter streams with offset-based parallelism", "dropout, noise injection and sampling kernels", "cross-device bit-exactness against the CPU reference", "throughput at multiple GB/s per device"]),
("fused_moe_kernel", "Fully Fused MoE Layer Kernel", "Route, gather, grouped-GEMM, activate, scatter, combine in one launch.", ["single-launch pipeline eliminating intermediate materialisation", "capacity overflow handling with deterministic drop/reroute", "expert-parallel and tensor-parallel variants", "end-to-end speedup measurement versus unfused baseline"]),
("fused_decode_step", "Whole-Decode-Step Persistent Kernel", "One persistent kernel per decode step: zero launch overhead (core of S3).", ["persistent grid with in-kernel layer loop and barrier synchronisation", "weight streaming and double buffering to hide memory latency", "in-kernel sampling and stop-condition evaluation", "per-token latency budget assertion with launch-overhead accounting"]),
("graph_capture", "Command-Graph Capture & Replay", "Captures static execution graphs to remove per-op dispatch cost.", ["capture/instantiate/replay lifecycle with shape-keyed cache", "dynamic-shape bucketing with padding cost analysis", "update-in-place of graph parameters between replays", "dispatch-overhead reduction measurement"]),
("mem_pool_device", "Device Memory Pool & Defragmenter", "Suballocation for accelerator memory with no fragmentation stalls.", ["stream-ordered pool with size classes and caching allocator", "defragmentation with relocation and handle indirection", "fragmentation metrics and worst-case bound proof", "OOM avoidance policy with graceful degradation"]),
("async_copy", "Asynchronous Copy & Pipelining Engine", "Overlaps all data movement with compute across the memory hierarchy.", ["multi-stage async copy pipelines with barrier tracking", "host-pinned and peer-to-peer transfer paths", "prefetch scheduling driven by the compiler's access model", "overlap efficiency measurement (achieved vs ideal)"]),
("cache_blocking", "Cache Blocking & Tiling Autotuner Hooks", "Exposes tunable tile parameters and cost models to the compiler tier.", ["parameterised tile space with legality constraints", "analytical cost model calibrated by measurement", "occupancy and register-pressure estimation", "tuning-time budget control"]),
("simd_cpu", "CPU SIMD Kernel Set", "AVX-512/AMX/NEON/SVE kernels for CPU-side and small-batch execution.", ["ISA-dispatched kernel variants selected at runtime", "AMX/matrix-extension tiling for CPU matmul", "cache-aware blocking with software prefetch", "cross-ISA numerical equivalence tests"]),
("numa_placement", "NUMA-Aware Placement & Migration", "Keeps every hot byte on the right socket.", ["topology discovery and distance-matrix construction", "first-touch and explicit migration policies", "cross-socket traffic accounting with remediation hints", "measured latency improvement on multi-socket hosts"]),
("bf16_stability", "Low-Precision Stability Toolkit", "Makes aggressive precision safe: loss scaling, error feedback, guards.", ["dynamic loss scaling with overflow detection and backoff", "error-feedback accumulators for repeated low-precision ops", "per-layer precision sensitivity profiler", "accuracy-preservation certification per precision plan"]),
("kernel_registry", "Kernel Registry & Runtime Dispatch", "Selects the best kernel variant for a given shape, dtype and device.", ["multi-key dispatch table with measured-performance priors", "shape-bucket interpolation and safe fallback ordering", "online performance feedback updating the priors", "dispatch overhead under 100 ns"]),
("kernel_verify", "Kernel Numerical Verification Suite", "Proves every kernel matches its reference within a declared bound.", ["reference implementations in exact/high precision", "error-bound derivation per kernel and per shape class", "adversarial input generation targeting cancellation and overflow", "certification artifact consumed by the eval tier"]),
("roofline_model", "Roofline & Performance Model", "Predicts achievable performance and flags kernels below target efficiency.", ["arithmetic-intensity computation from the graph IR", "hierarchical roofline including cache and interconnect ceilings", "gap analysis producing actionable kernel work items", "prediction accuracy validation against measurements"]),
("profiler_hooks", "Kernel Profiling & Counter Collection", "Low-overhead hardware-counter collection for every kernel.", ["counter-set selection with multiplexing and normalisation", "per-kernel attribution with negligible perturbation", "flamegraph-ready output and per-tier rollup", "overhead measurement proving <1% perturbation"]),
("emulation_reference", "Bit-Exact CPU Emulation of All Kernels", "A slow, obviously-correct implementation of every kernel for differential testing.", ["scalar reference for each kernel with identical rounding semantics", "emulated FP8/INT4 arithmetic in software", "differential test driver over random and adversarial shapes", "documented divergence policy where exactness is impossible"]),
("kernel_fusion_rules", "Fusion Legality & Rewrite Rules", "The rule set the compiler uses to combine kernels safely.", ["legality predicates covering aliasing, precision and determinism", "profitability estimation from the roofline model", "rule confluence and termination argument", "rule-set regression tests with golden fused plans"]),
("collective_local", "Intra-Node Collectives", "NVLink/PCIe-class all-reduce, all-gather, reduce-scatter within a node.", ["ring and tree algorithms with size-based selection", "in-place and fused-with-compute variants", "deterministic reduction order across ranks", "bandwidth efficiency versus theoretical link peak"]),
("p2p_transfer", "Peer-to-Peer Device Transfer", "Direct device-to-device movement bypassing the host.", ["peer access setup with capability probing and fallback", "chunked pipelined transfers with overlap", "topology-aware path selection", "latency/bandwidth matrix measurement"]),
("stream_scheduler", "Stream & Event Scheduling", "Concurrency across device queues without deadlock or serialisation stalls.", ["dependency-driven stream assignment with event fencing", "priority streams for latency-critical decode work", "deadlock freedom argument and cycle detection", "achieved-concurrency measurement"]),
("power_thermal", "Power & Thermal-Aware Kernel Governance", "Sustains peak throughput without thermal or power throttling collapse.", ["power/clock telemetry ingestion and throttle prediction", "kernel-mix scheduling to smooth power draw", "per-joule efficiency accounting per kernel", "sustained-load endurance benchmark"]),
("error_correction", "Hardware Fault Detection & Correction", "Silent-data-corruption defence at the kernel layer.", ["ECC/parity telemetry and error-rate tracking", "algorithm-based fault tolerance checksums in GEMM", "recompute-on-mismatch policy with cost accounting", "injected-fault detection-rate measurement"]),
("kernel_codegen_rt", "Runtime Kernel Code Generation & JIT Cache", "Emits specialised kernels at runtime and caches them persistently.", ["template instantiation with constant folding of shapes", "on-disk content-addressed JIT cache with version keys", "compile-time budget control and background warmup", "cold/warm start latency measurement"]),
("sparse_attention_kernels", "Sparse & Hierarchical Attention Kernels", "Block-sparse, top-k and landmark attention for long context (part of S2).", ["block-sparsity index construction from importance scores", "top-k key selection fused with the attention loop", "landmark/summary-token hierarchical attention", "quality-vs-FLOP curves at multiple sparsity levels"]),
("embedding_kernels", "Embedding & Unembedding Kernels", "Large-vocabulary lookup and logit projection with tied weights.", ["sharded vocabulary gather with load balancing", "fused unembed + top-k + sampling avoiding full-logit materialisation", "vocab-parallel reduction with deterministic order", "memory-traffic reduction measurement"]),
("mask_engine", "Mask Construction & Compression Engine", "Builds every attention/loss mask form without materialising dense masks.", ["algebraic mask representation (causal, window, doc, arbitrary) with composition", "compressed block-mask encoding consumed by attention kernels", "packed-sequence document boundary handling", "equivalence tests against dense masks"]),
("tensor_view", "Zero-Copy Tensor View Algebra", "Slicing, reshaping and striding without copies or surprises.", ["stride algebra with contiguity and overlap analysis", "safe-view proofs preventing aliasing bugs", "lazy materialisation policy with cost estimation", "exhaustive view-composition tests"]),
("ragged_batch", "Ragged & Packed Batch Kernels", "Variable-length sequences with no padding waste.", ["offset-based ragged layout with cumulative-length metadata", "packing solver minimising waste under a token budget", "kernels operating directly on ragged layout", "padding-waste reduction measurement"]),
("kernel_bench_suite", "Kernel Benchmark Suite & Baselines", "The canonical performance corpus that gates every kernel change.", ["shape corpus covering prefill, decode, training and MoE regimes", "baseline records with variance bands and machine fingerprints", "regression detection with statistical significance", "public-comparable summary tables"]),
("micro_opt_catalog", "Micro-Optimisation Catalogue", "The documented, tested set of low-level tricks with measured payoffs.", ["instruction-level: unrolling, pipelining, dual-issue, predication", "memory-level: vectorisation width, prefetch distance, bank conflicts", "each entry ships a before/after benchmark and applicability predicate", "anti-pattern list with measured harm"]),
("tensor_core_util", "Tensor-Core Utilisation Optimiser", "Drives matrix-unit occupancy toward the hardware ceiling.", ["fragment layout and register-pressure planning", "pipeline-depth selection versus shared-memory capacity", "utilisation measurement with stall-reason attribution", "target: >=88% of peak on the benchmark corpus"]),
("kernel_docs_spec", "Kernel Contract Specification & Docs Generator", "Machine-readable specs for all kernels: semantics, bounds, budgets.", ["formal pre/postcondition notation per kernel", "error-bound and determinism declarations", "auto-generated documentation and dispatch tables", "spec-vs-implementation conformance checker"]),
]
