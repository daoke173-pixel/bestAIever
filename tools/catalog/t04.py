PARTS = [
("device_abstraction", "Device Abstraction Layer", "One uniform device interface over every accelerator vendor and generation.", ["device enumeration, capability probing and feature-flag normalisation", "context/queue/event lifecycle with RAII-style safety", "vendor backend trait with two reference implementations", "conformance suite every backend must pass"]),
("topology_discovery", "Interconnect Topology Discovery", "Builds an exact machine graph: links, bandwidths, latencies, failure domains.", ["PCIe/NVLink/fabric enumeration with bandwidth probing", "multi-node topology assembly from per-node views", "distance and bisection-bandwidth computation", "topology-vs-measurement validation and anomaly flagging"]),
("collective_global", "Multi-Node Collective Library", "Cluster-scale all-reduce/all-gather/all-to-all with topology awareness.", ["hierarchical algorithms (intra-node then inter-node) with size-based selection", "all-to-all optimisation for expert parallelism", "deterministic reduction ordering across thousands of ranks", "scaling efficiency measurement to 100k ranks"]),
("rdma_transport", "RDMA & Zero-Copy Network Transport", "The lowest-latency path for tensors between machines.", ["queue-pair management, memory registration and completion handling", "zero-copy send/recv with pre-registered buffer pools", "congestion control interaction and loss recovery", "latency/bandwidth benchmark versus hardware line rate"]),
("tcp_fallback", "Reliable TCP/QUIC Transport Fallback", "Works everywhere, degrades predictably when RDMA is unavailable.", ["framing, multiplexing and flow control over stream transports", "QUIC path with 0-RTT reconnect and stream priorities", "adaptive chunk sizing and pacing", "measured gap versus RDMA with documented degradation ladder"]),
("comm_scheduler", "Communication Scheduling & Prioritisation", "Prevents communication from ever becoming the critical path.", ["priority classes for latency-critical versus bulk transfers", "bandwidth allocation with work-conserving fairness", "deadline-aware scheduling tied to the task DAG", "measured critical-path improvement"]),
("expert_parallel_fabric", "Expert-Parallel Routing Fabric", "Moves tokens to experts across the cluster at MoE speed.", ["token dispatch/combine with all-to-all and capacity control", "locality-aware expert placement to shorten hops", "skew handling with overflow rerouting", "throughput at 512 experts across many nodes"]),
("sequence_parallel_fabric", "Sequence & Context Parallel Fabric", "Splits a single 1M-token context across machines.", ["ring/striped context parallelism with overlapped attention communication", "KV shard placement and migration policy", "load balance under causal masking", "scaling efficiency for very long contexts"]),
("pipeline_fabric", "Pipeline Stage Transport", "Activation and gradient movement between pipeline stages with no bubbles.", ["point-to-point transfer with double buffering and prefetch", "stage-boundary compression for activation traffic", "backpressure integration with the pipeline schedule", "bubble-fraction measurement"]),
("param_server_shard", "Sharded Parameter Store", "Where the weights live, and how they get to compute fast.", ["shard mapping with replication factor and placement policy", "fast broadcast/gather of shards with topology awareness", "hot-shard replication for skewed access", "load time for a full model at cluster scale"]),
("weight_streaming", "Weight Streaming & Tiering", "Runs models larger than device memory without stalling.", ["prefetch schedule derived from the execution plan", "multi-tier residency (device/host/NVMe) with cost-aware placement", "double-buffered layer streaming overlapped with compute", "stall-time measurement at multiple memory ratios"]),
("nvme_offload", "NVMe / Storage Offload Engine", "High-throughput spill for KV caches, activations and cold weights.", ["direct-IO with queue depth tuning and alignment handling", "GPU-direct storage paths where available", "wear-aware write policy and lifetime accounting", "achieved throughput versus device specification"]),
("host_pinned_pool", "Host Pinned Memory Manager", "Staging memory that never becomes a bottleneck or a leak.", ["pinned pool with size classes and NUMA affinity", "registration caching across transfers", "leak/exhaustion detection with actionable diagnostics", "transfer-rate measurement versus unpinned baseline"]),
("device_health", "Device Health Monitoring & Prediction", "Detects a degrading accelerator before it corrupts a run.", ["telemetry ingestion: ECC, throttling, clocks, link errors, temperature", "anomaly detection and remaining-useful-life estimation", "pre-emptive drain recommendation with cost/benefit", "detection-rate evaluation on injected degradation"]),
("fault_domains", "Fault Domain Modelling & Placement", "Places replicas and shards so no single failure stops the assembly.", ["failure-domain hierarchy (device, host, rack, zone) modelling", "correlated-failure-aware placement solver", "blast-radius computation for any single failure", "simulated-failure survival testing"]),
("elastic_scaling", "Elastic Membership & Rescaling", "Adds or removes machines mid-run without restarting.", ["membership protocol with epochs and consistent views", "state redistribution with minimal data movement", "in-flight request preservation during rescale", "rescale latency and throughput-dip measurement"]),
("checkpoint_transport", "Distributed Checkpoint IO", "Saves and restores petabyte-scale state fast and safely.", ["sharded parallel write with aggregation to avoid small-file storms", "async checkpointing overlapped with compute", "integrity verification and partial-corruption recovery", "checkpoint/restore wall-clock measurement at scale"]),
("failure_recovery", "Failure Detection & Fast Restart", "Turns a machine loss into a small hiccup rather than a lost run.", ["fast failure detection with false-positive suppression", "in-memory redundant state for immediate recovery", "partial restart limited to affected pipeline stages", "mean-time-to-recovery measurement under injected faults"]),
("straggler_mitigation", "Straggler Detection & Mitigation", "One slow machine must not slow 100k others.", ["per-rank latency distribution tracking with robust statistics", "speculative duplicate execution with first-wins semantics", "work stealing/rebalancing across ranks", "tail-latency improvement measurement"]),
("power_cluster", "Cluster Power & Energy Governance", "Maximises throughput per watt within facility limits.", ["power-cap-aware scheduling and frequency policy", "energy accounting per request and per part", "thermal-headroom-aware placement", "measured throughput-per-joule improvement"]),
("network_congestion", "Congestion Control & Traffic Engineering", "Keeps the fabric out of collapse under all-to-all storms.", ["adaptive routing and multipath spraying with reordering handling", "incast avoidance via pacing and receiver-driven scheduling", "congestion telemetry and hot-link detection", "goodput measurement under adversarial traffic patterns"]),
("multi_tenancy", "Multi-Tenant Isolation & QoS", "Many workloads on one cluster with hard performance isolation.", ["resource partitioning with enforcement at device and fabric level", "QoS classes with latency SLOs and admission control", "noisy-neighbour detection and mitigation", "SLO-attainment measurement under contention"]),
("device_virtualisation", "Device Partitioning & Virtualisation", "Slices accelerators for small requests without waste.", ["partition creation, sizing policy and lifecycle", "performance-isolation verification between partitions", "packing solver maximising utilisation under SLOs", "utilisation improvement measurement"]),
("heterogeneous_pool", "Heterogeneous Hardware Pooling", "Uses every generation and vendor of accelerator effectively together.", ["capability-aware work routing with per-device cost models", "mixed-generation collective handling with slowest-link awareness", "graceful capability degradation for older devices", "aggregate-throughput measurement across mixed pools"]),
("edge_runtime", "Edge & On-Device Runtime", "Runs the distilled fast paths on laptops, phones and embedded targets.", ["tight memory-budget execution with streaming weights", "battery/thermal-aware scheduling", "capability subset declaration and cloud handoff protocol", "latency/quality measurements on constrained targets"]),
("cloud_provisioner", "Capacity Provisioning & Placement Planner", "Decides what hardware to acquire and where to run each workload.", ["demand forecasting from request traces", "cost/latency/carbon multi-objective placement", "spot/preemptible capacity strategy with checkpoint coupling", "cost-per-request reduction measurement"]),
("scheduler_cluster", "Cluster Job Scheduler Integration", "Plays well with Slurm/Kubernetes-class schedulers without losing control.", ["gang scheduling and topology-aware allocation requests", "preemption handling with fast drain and resume", "queue-time prediction and backfill exploitation", "allocation-quality measurement (topology compactness)"]),
("service_discovery", "Service Discovery & Membership Registry", "How 1000 parts across many machines find each other.", ["gossip/consensus-backed registry with fast convergence", "capability-indexed lookup with locality preference", "partition tolerance behaviour and split-brain avoidance", "convergence-time measurement at 100k nodes"]),
("consensus_core", "Lightweight Consensus & Leader Election", "Consistency for control-plane decisions only, never in the hot path.", ["Raft-style log replication with pre-vote and joint consensus", "lease-based leadership with clock-skew safety", "linearisability testing under partitions", "control-plane latency budget enforcement"]),
("distributed_lock", "Distributed Locking & Fencing", "Prevents two workers from ever doing the same irreversible thing.", ["lease locks with monotonic fencing tokens", "lock-holder failure detection and safe takeover", "deadlock avoidance with ordered acquisition", "safety verification under process pauses and clock jumps"]),
("clock_sync", "Cluster Clock Synchronisation", "Enough time agreement to reason about causality and deadlines.", ["PTP/NTP ingestion with uncertainty intervals", "hybrid logical clocks for causal ordering", "skew-bound enforcement in deadline arithmetic", "measured uncertainty and its effect on SLOs"]),
("data_locality", "Data Locality & Cache Affinity Router", "Routes work to where the bytes already are.", ["consistent hashing with bounded loads and affinity keys", "KV-cache-aware routing for multi-turn sessions", "migration cost estimation versus remote-access cost", "cache-hit-rate improvement measurement"]),
("bandwidth_accounting", "Bandwidth & Interconnect Accounting", "Makes every byte moved visible and attributable.", ["per-link, per-tier, per-request byte accounting", "attribution to parts and phases with negligible overhead", "budget enforcement on communication volume", "report used by the compiler to reduce traffic"]),
("secure_channel", "Encrypted Inter-Node Channels", "Confidentiality and integrity without losing throughput.", ["mutual authentication with short-lived certificates", "AEAD framing with hardware offload where available", "key rotation without connection drops", "throughput cost measurement of encryption"]),
("confidential_compute", "Confidential Computing Integration", "Runs sensitive workloads inside attested enclaves.", ["attestation verification and policy binding", "encrypted memory handling and IO shim overhead management", "key release conditioned on measurement", "performance overhead measurement and mitigation"]),
("firmware_compat", "Driver & Firmware Compatibility Matrix", "Prevents the classic 'works on that node' failure class.", ["version detection, known-issue database and hard gating", "capability differences normalised into the target description", "pre-flight validation suite run at node admission", "matrix coverage report"]),
("nic_offload", "Network Offload & In-Network Compute", "Pushes reductions and packet work into NICs and switches.", ["in-network all-reduce integration with fallback", "kernel-bypass datapath with poll-mode threads", "offload correctness verification versus host computation", "measured host-CPU and latency savings"]),
("device_reset", "Device Reset & Isolation Recovery", "Recovers a wedged accelerator without rebooting the host.", ["hang detection with watchdogs and progress counters", "safe reset sequencing and state reconstruction", "poison propagation prevention to healthy devices", "recovery-success-rate measurement under injected hangs"]),
("numa_topology_cluster", "Host-Level Resource Topology Binding", "Binds threads, memory, devices and NICs coherently.", ["CPU/GPU/NIC affinity solver from the topology graph", "IRQ and interrupt-affinity configuration guidance", "hugepage and transparent-hugepage policy", "measured latency improvement from correct binding"]),
("telemetry_transport", "High-Volume Telemetry Pipeline", "Ships metrics, traces and events from 100k nodes without cost explosion.", ["local aggregation and pre-summarisation before egress", "adaptive sampling with error-bound guarantees", "backpressure and loss accounting", "telemetry cost per request measurement"]),
("simulation_cluster", "Cluster Simulator", "Tests distributed logic at 100k scale on one machine.", ["discrete-event simulation of compute, memory and network", "fault, straggler and partition injection scenarios", "calibration against real measurements with error reporting", "scenario library covering all failure modes"]),
("capacity_benchmarks", "Distributed Benchmark Suite", "The canonical scaling measurements for the whole platform.", ["strong/weak scaling harnesses with statistical rigor", "communication microbenchmarks per link type", "end-to-end throughput and latency at multiple scales", "baseline records with machine fingerprints"]),
("cost_model_cluster", "Total-Cost-of-Serving Model", "Turns hardware, power and time into dollars per request.", ["cost decomposition per device-hour, byte moved and joule", "amortisation of compilation, warmup and idle capacity", "what-if analysis for hardware and plan changes", "validation against measured billing data"]),
("request_router", "Global Request Router & Load Balancer", "Sends each request to the cheapest machine that meets its SLO.", ["latency-aware, cache-affinity-aware, cost-aware routing", "queue-length and power-of-two-choices balancing", "SLO-class routing with overflow tiers", "tail-latency and cost measurement under mixed load"]),
("region_failover", "Multi-Region Replication & Failover", "Survives losing an entire datacentre.", ["state replication strategy with RPO/RTO targets", "traffic shifting with connection draining", "consistency model documentation and enforcement", "failover drill results and measured RTO"]),
("hw_sw_codesign", "Hardware/Software Co-Design Specification", "The precise ask to hardware vendors, derived from measured bottlenecks.", ["bottleneck attribution from roofline and profiling data", "quantified feature requests with projected speedups", "custom-accelerator ISA proposal for the hottest kernels", "projection methodology and validation plan"]),
("green_scheduling", "Carbon-Aware Scheduling", "Same work, less carbon, no SLO loss.", ["carbon-intensity signal ingestion and forecasting", "deferrable-workload shifting under SLO constraints", "carbon accounting per request", "measured emissions reduction at constant SLO attainment"]),
("device_alloc_fair", "Fair-Share Device Allocation", "Long-term fairness across teams and workloads.", ["dominant-resource fairness across heterogeneous devices", "borrowing/lending with reclaim guarantees", "starvation-freedom proof and monitoring", "fairness metric measurement over long horizons"]),
("hpc_interop", "HPC & Scientific Stack Interoperability", "Works inside existing supercomputing environments.", ["MPI-compatible bootstrap and communicator mapping", "shared-filesystem-friendly IO patterns", "job-script generation for major HPC sites", "validated runs on multiple site configurations"]),
("provision_verify", "Node Admission & Continuous Verification", "Every machine proves it is healthy and correct before and during service.", ["burn-in suite: numeric correctness, bandwidth, thermal, memory integrity", "continuous lightweight verification during production", "automatic quarantine on verification failure", "escape-rate measurement of bad nodes reaching production"]),
]
