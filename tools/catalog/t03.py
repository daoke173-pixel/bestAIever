PARTS = [
("ir_core", "Ω-IR Core Data Structures", "The SSA-based graph IR that all compilation and optimisation operates on.", ["typed SSA with regions, blocks and explicit effect edges", "shape/dtype/device/determinism attributes on every value", "structural hashing and interning for O(1) equality", "verifier catching every malformed-IR class with precise diagnostics"]),
("ir_builder", "IR Construction & Tracing Frontend", "Captures model code into Ω-IR without behavioural surprises.", ["tracing with control-flow capture and guard generation", "symbolic-shape propagation during capture", "side-effect ordering preservation and explicit barriers", "trace-vs-eager equivalence differential tests"]),
("ir_printer", "IR Textual Format, Parser & Round-Trip", "Human-readable IR that round-trips exactly, essential for 1000-way debugging.", ["stable textual syntax with attribute pretty-printing", "parser accepting the full grammar with error recovery", "round-trip fidelity property test over random IR", "diff-friendly canonical ordering"]),
("pass_manager", "Pass Manager & Optimisation Pipelines", "Orchestrates passes with dependency, invalidation and instrumentation support.", ["analysis caching with precise invalidation", "pipeline description language and preset pipelines", "per-pass timing, IR-size and diagnostic capture", "pass-ordering search harness for pipeline tuning"]),
("shape_inference", "Symbolic Shape Inference & Constraint Solver", "Resolves dynamic shapes to a small set of specialised plans.", ["symbolic dimension algebra with guard accumulation", "constraint solving for divisibility and bounds", "shape-bucket selection minimising padding and recompiles", "unsat diagnosis pointing at the offending op"]),
("dtype_promotion", "Precision Assignment & Mixed-Precision Planner", "Chooses the cheapest precision per op that still meets the accuracy contract.", ["sensitivity analysis via perturbation and gradient proxies", "ILP/greedy solver over the precision-assignment space", "accuracy constraint verification against a reference run", "measured speedup at certified accuracy parity"]),
("algebraic_rewrite", "Algebraic Simplification & Rewrite Engine", "The rule-based optimiser: hundreds of semantics-preserving rewrites.", ["e-graph saturation with cost-based extraction", "rule library covering linear algebra, softmax, norms and gating identities", "confluence/termination handling with iteration limits", "semantics-preservation tests per rule"]),
("fusion_pass", "Operator Fusion Pass", "Merges ops into single-pass kernels using the T02 legality rules.", ["fusion group formation with cut-based partitioning", "profitability model from arithmetic intensity and memory traffic", "recomputation-vs-materialisation decisions", "measured memory-traffic reduction on the benchmark corpus"]),
("layout_assignment", "Layout Assignment & Relayout Minimisation", "Picks tensor layouts globally to eliminate transposes.", ["layout constraint propagation across the whole graph", "min-cut formulation for relayout placement", "blocked/padded layout selection for tensor cores", "count of eliminated relayouts as the gate metric"]),
("memory_planner", "Static Memory Planner & Liveness Allocator", "Assigns every buffer an offset with minimal peak memory.", ["liveness analysis with interval graph construction", "best-fit-by-size allocation with proven bounds", "in-place and aliasing opportunity detection", "peak-memory reduction versus naive allocation"]),
("rematerialisation", "Activation Rematerialisation Planner", "Trades compute for memory optimally during training and long-context inference.", ["cost-model-driven checkpoint selection (dynamic programming)", "segment-based recompute schedules with nested checkpoints", "memory-budget-constrained optimisation", "measured memory/compute Pareto frontier"]),
("scheduler_pass", "Instruction Scheduling & Overlap Pass", "Orders operations to hide latency and maximise concurrency.", ["list scheduling with a machine model of latencies and ports", "communication/compute overlap insertion", "critical-path shortening with resource constraints", "achieved-overlap metric versus theoretical ideal"]),
("parallel_partition", "Automatic Parallelism Partitioner", "Derives tensor/pipeline/expert/sequence parallel plans automatically.", ["sharding-propagation solver over annotated inputs and outputs", "cost model including collective volume and bubble time", "hybrid-plan search across 4 parallelism axes", "plan quality versus hand-tuned expert baselines"]),
("pipeline_planner", "Pipeline Parallel Schedule Synthesiser", "Generates bubble-minimal pipeline schedules.", ["1F1B, interleaved and zero-bubble schedule generation", "stage-balancing partitioner using measured per-layer costs", "activation-memory accounting per schedule", "bubble-fraction measurement and target enforcement"]),
("collective_insertion", "Collective Communication Insertion & Fusion", "Places, fuses and overlaps every collective operation.", ["all-reduce/all-gather/reduce-scatter insertion from sharding specs", "collective fusion with bucketing to amortise latency", "overlap with compute via async handles and dependency edges", "communication-volume reduction measurement"]),
("autotuner_core", "Autotuning Search Engine", "Finds the best kernel configuration per shape with a small measurement budget.", ["search over tile/stage/precision spaces with pruning by legality and cost model", "bayesian/bandit search with transfer from similar shapes", "measurement harness with noise handling and early stopping", "tuning-budget-vs-quality curves"]),
("cost_model_learned", "Learned Cost Model", "Predicts kernel runtime accurately enough to skip most measurements.", ["feature extraction from IR and target description", "regression model with uncertainty estimates for search guidance", "online updating from production measurements", "prediction-error target under 8% on held-out shapes"]),
("codegen_gpu", "Accelerator Code Generator", "Emits high-performance device code from scheduled Ω-IR.", ["tile-level IR lowering with explicit memory-hierarchy staging", "async-copy/barrier/pipeline emission and register allocation hints", "vendor-neutral backend interface with two concrete targets", "generated-vs-handwritten performance comparison"]),
("codegen_cpu", "CPU Code Generator", "Vectorised CPU codegen for small-batch, edge and reference execution.", ["loop vectorisation with ISA-specific intrinsic selection", "cache blocking and prefetch insertion from the machine model", "multi-threading with deterministic work partitioning", "efficiency versus tuned BLAS baselines"]),
("jit_cache_compiler", "Compilation Cache & Warm Start", "Never compiles the same thing twice, across processes and machines.", ["content-addressed cache keyed by IR hash + target + flags", "shared cache protocol safe for concurrent writers", "profile-guided precompilation of the hot shape set", "cold-start compile-time reduction measurement"]),
("guard_specialise", "Guarded Specialisation & Recompilation Policy", "Specialises aggressively while staying correct under shape/flag changes.", ["guard generation, minimisation and cheap runtime evaluation", "specialisation-cache with LRU and recompile-storm prevention", "deoptimisation path preserving semantics", "guard-overhead measurement under 1% of step time"]),
("kernel_selection", "Global Kernel Selection Optimiser", "Chooses kernels jointly rather than greedily, accounting for layout coupling.", ["joint selection formulated over the layout/precision/kernel product space", "beam search with cost-model pruning", "constraint handling for determinism and accuracy modes", "end-to-end gain versus greedy selection"]),
("graph_partition_device", "Device Placement & Graph Partitioning", "Assigns subgraphs to heterogeneous devices under memory and bandwidth limits.", ["multi-constraint partitioning with communication minimisation", "heterogeneous-device cost modelling", "memory-feasibility checking with rematerialisation coupling", "placement quality versus exhaustive search on small graphs"]),
("dynamic_shapes", "Dynamic Shape Execution Strategy", "Handles unbounded shape variety with bounded compilation cost.", ["bucketing policy with padding-cost/recompile-cost tradeoff", "fully dynamic fallback kernels with acceptable efficiency", "shape-distribution learning from production traces", "tail-latency impact measurement"]),
("differentiation", "Automatic Differentiation on Ω-IR", "Reverse and forward mode AD with checkpointing integration.", ["reverse-mode transformation with correct effect and control-flow handling", "forward-mode and JVP/VJP composition for higher-order needs", "custom-gradient registration and verification", "gradient correctness versus numerical differentiation"]),
("vectorisation_transform", "Batching & Vectorisation Transform", "Automatic vmap-style batching over arbitrary subgraphs.", ["rank-polymorphic transformation with batching rules per op", "nested batching composition", "fallback loop generation with clear performance warnings", "equivalence tests against explicit loops"]),
("loop_transform", "Polyhedral Loop Transformation", "Tiling, fusion, interchange and skewing with dependence legality.", ["dependence analysis and legality checking", "tiling/skewing/interchange transformation composition", "objective functions for locality and parallelism", "transformation legality proofs on a benchmark set"]),
("constant_folding", "Constant Folding & Weight Preprocessing", "Precomputes everything that does not depend on the request.", ["exact constant evaluation with the same numerics as runtime", "weight transformation baking (layout, quantisation, fusion of scales)", "memory-bounded folding policy", "startup-time and per-request savings measurement"]),
("dead_code_dce", "Dead Code, CSE & Canonicalisation", "The classic cleanups, made effect-aware and determinism-safe.", ["effect-aware DCE preserving observable behaviour", "global value numbering and common-subexpression elimination", "canonicalisation to a normal form enabling later matches", "IR-size and op-count reduction metrics"]),
("effect_system", "Effect & Purity Analysis", "Tracks IO, randomness, mutation and safety effects through the graph.", ["effect lattice with precise join semantics", "purity inference enabling reordering and caching", "randomness-source tracking tied to split_seed", "misclassification detection tests"]),
("ir_verifier", "IR Verification & Invariant Checking", "Catches compiler bugs before they become model bugs.", ["structural, type, shape, effect and determinism invariants", "pass-boundary verification with cheap and thorough modes", "counterexample minimisation for failed invariants", "verifier coverage over a mutation-generated bad-IR corpus"]),
("differential_testing", "Compiler Differential Test Harness", "Proves optimised execution matches reference execution exactly.", ["random IR generation with realistic op distributions", "optimisation-level differential comparison with tolerance classes", "bisection over the pass pipeline to localise miscompiles", "nightly-scale corpus with pinned regressions"]),
("compile_time_budget", "Compilation Time Budget Manager", "Keeps compile time predictable, even under aggressive optimisation.", ["per-pass time budgeting with graceful degradation", "priority ordering so the hottest subgraphs get the most effort", "background/incremental compilation with hot swap", "compile-time distribution measurement and tail control"]),
("profile_guided", "Profile-Guided Optimisation Pipeline", "Feeds production behaviour back into compilation decisions.", ["execution profile collection and aggregation format", "hot-path specialisation and branch-probability propagation", "profile-staleness detection and safe reuse", "measured gain from PGO versus static heuristics"]),
("target_description", "Target Machine Description Language", "One declarative description per accelerator drives all target-specific decisions.", ["hierarchical description: compute units, memories, bandwidths, latencies, ISA features", "capability queries used by passes instead of hardcoded checks", "validation of a description against measured microbenchmarks", "new-target onboarding checklist and conformance suite"]),
("ir_serialization", "IR Serialisation & Portable Artifact Format", "Compiled artifacts that ship, version and verify.", ["compact binary format with schema versioning and forward compat", "embedded provenance, guards and target requirements", "signature and integrity verification before load", "cross-version load tests"]),
("bytecode_vm", "Ω Execution Bytecode & Interpreter", "The portable, debuggable execution path used as the correctness oracle.", ["compact bytecode with explicit buffer and effect operands", "interpreter with single-step, breakpoint and watch support", "identical numerics to compiled paths in reference mode", "interpretive overhead documented and bounded"]),
("debug_symbols", "Debug Info, Source Mapping & Blame", "Maps any runtime anomaly back to a source line in one of 1000 files.", ["source-location propagation surviving every transformation", "inlining and fusion attribution with multi-origin support", "blame report generation for numerical anomalies", "attribution accuracy tests through deep pass pipelines"]),
("pass_search", "Automated Pass Pipeline Search", "Discovers better optimisation pipelines than hand-written ones.", ["pipeline space definition with legality and idempotence constraints", "evolutionary/bandit search with early stopping", "generalisation validation across model families", "measured improvement over the default pipeline"]),
("superoptimiser", "Bounded Superoptimiser for Hot Kernels", "Exhaustively searches for optimal instruction sequences on small critical regions.", ["region selection by profile contribution and size bound", "enumerative/SMT-based equivalent-sequence search", "verified equivalence before adoption", "catalogue of discovered sequences with measured wins"]),
("equality_saturation", "E-Graph Engine & Extraction", "The rewriting substrate: saturate, then extract the cheapest equivalent program.", ["congruence-closure e-graph with efficient rebuild", "cost function plugins including learned costs", "extraction with cycle avoidance and global optimality bounds", "saturation-limit policies and quality-vs-time curves"]),
("kernel_template_lib", "Kernel Template Library", "Parameterised templates the codegen instantiates for any shape.", ["template families for GEMM, attention, scan, reduction, elementwise", "parameter validity constraints and default heuristics", "instantiation cost and code-size control", "coverage report over the benchmark shape corpus"]),
("linker_omega", "Ω-Linker for 1000 Compiled Parts", "Links independently compiled part artifacts into one executable assembly.", ["capability symbol resolution across artifact boundaries", "cross-part inlining and whole-assembly optimisation", "version/ABI compatibility enforcement at link time", "link-time report: unresolved, duplicate, degraded capabilities"]),
("incremental_build", "Incremental Compilation & Change Impact", "Recompiles only what 1 changed part affects, across the 1000-part assembly.", ["fine-grained dependency graph with content hashing", "change-impact analysis and minimal rebuild set computation", "cache reuse across branches and machines", "rebuild-time measurement for single-part edits"]),
("crosscompile", "Cross-Compilation & Target Matrix", "Build once, run on every supported accelerator and CPU.", ["hermetic toolchain description and pinned versions", "multi-target artifact bundling with runtime selection", "reproducible-build verification (identical bytes)", "target-matrix CI definition"]),
("numeric_mode_lowering", "Determinism-Mode Lowering", "Compiles the same graph into deterministic or fast variants with declared differences.", ["mode-aware lowering rules for reductions, atomics and scheduling", "cost-of-determinism measurement per model region", "mode conformance verification (bit-exactness in deterministic mode)", "policy for mixed-mode graphs"]),
("op_registry", "Operator Registry & Semantics Database", "The canonical definition of every operator in Ω-IR.", ["op schema: signature, semantics, effects, error bounds, determinism", "reference implementation link and lowering candidates", "registry consistency checks and completeness audit", "documentation generation for all operators"]),
("graph_diff_tool", "Graph Diff & Optimisation Explainer", "Explains, in human terms, what the compiler did and why.", ["structural graph diff with matching heuristics", "per-decision rationale capture during passes", "human-readable optimisation report with measured impact", "explanation fidelity tests against pass logs"]),
("compiler_fuzzer", "Compiler Fuzzer & Miscompile Hunter", "Continuously attacks the compiler with generated programs.", ["grammar-based IR generation with semantic validity", "metamorphic testing (equivalent programs must agree)", "automatic reduction of miscompile cases", "found-bug corpus with pinned regression tests"]),
("build_provenance", "Build Provenance & Reproducibility Attestation", "Every artifact proves exactly how it was produced.", ["full input hashing: sources, toolchains, flags, target descriptions", "in-toto-style attestation generation and verification", "bit-identical rebuild verification across machines", "provenance query interface for audits"]),
]
