PARTS = [
("engine_core", "Inference Engine Core Loop", "The top-level serving loop: admit, schedule, step, emit, retire.", ["single-threaded-decision / multi-threaded-execution architecture", "step-loop with bounded per-iteration work for predictable latency", "state machine per sequence with explicit legal transitions", "throughput and p99 latency budget assertions"]),
("continuous_batching", "Continuous Batching Scheduler", "Never waits for a batch: sequences join and leave every step.", ["iteration-level scheduling with join/leave at any step", "batch-composition policy balancing prefill and decode work", "fairness and starvation prevention across sequences", "measured throughput gain versus static batching"]),
("chunked_prefill", "Chunked Prefill & Prefill/Decode Interleaving", "Long prompts never block short interactive requests.", ["prompt chunking with configurable token budget per step", "prefill/decode mixing policy tuned for latency targets", "TTFT versus throughput tradeoff measurement", "interference measurement on interactive requests"]),
("prefill_decode_split", "Disaggregated Prefill/Decode Serving", "Separate hardware pools for the two very different phases.", ["KV handoff protocol between prefill and decode nodes", "pool sizing policy from traffic characteristics", "handoff latency and bandwidth accounting", "cost-per-token reduction measurement versus colocated serving"]),
("admission_control", "Admission Control & Queue Management", "Says no early rather than failing slowly.", ["SLO-aware admission with predicted-latency rejection", "priority queues with aging and deadline scheduling", "queue-depth control keeping latency bounded under overload", "measured SLO attainment during 5x overload"]),
("cascade_stage1_draft", "Cascade Stage 1: 1.5B Draft Engine", "The fastest drafter: cheap tokens for the easy majority.", ["ultra-low-latency decode loop with fused persistent kernel", "acceptance-rate measurement and self-tuning draft length", "memory co-residency with the core model", "contribution measurement to the S1 4.0x factor"]),
("cascade_stage2_draft", "Cascade Stage 2: 7B Draft Engine", "The middle drafter: higher acceptance on harder spans.", ["shared-KV-layout draft with the core for zero-copy verification", "adaptive escalation from stage 1 based on confidence", "per-domain acceptance-rate tables", "latency/acceptance tradeoff calibration"]),
("cascade_stage3_draft", "Cascade Stage 3: 70B Draft Engine", "The near-core drafter for the hardest content.", ["high-acceptance drafting with multi-token prediction heads", "cost-benefit gating: only used when it pays off", "acceptance measurement on reasoning-heavy traffic", "budget accounting versus direct core decoding"]),
("cascade_tree_verify", "Tree-Structured Speculative Verification", "Verifies many candidate continuations in one core forward pass.", ["candidate-tree construction with beam and topology policy", "tree attention mask construction and batched verification", "longest-accepted-path extraction with exact-distribution preservation", "measured tokens-accepted-per-core-pass"]),
("cascade_acceptance", "Acceptance Sampling & Distribution Equivalence", "Proves speculation does not change the output distribution.", ["modified rejection sampling with exactness proof", "typical-acceptance and lossy modes with declared divergence bounds", "statistical equivalence testing versus non-speculative decoding", "acceptance-rate optimisation under equivalence constraints"]),
("cascade_scheduler", "Cascade Stage Selection Policy", "Picks the right drafter for the current span, per request.", ["online bandit selection over stages with cost/acceptance feedback", "content-type and difficulty signals driving selection", "policy stability and oscillation prevention", "end-to-end speedup attribution per stage"]),
("cascade_batching", "Batched Speculative Execution", "Speculation that gets better, not worse, with batch size.", ["per-sequence variable draft lengths within one batch", "ragged verification with no padding waste", "batch-wide acceptance accounting and rebalancing", "throughput measurement at batch sizes 1 to 1024"]),
("cascade_rollback", "Speculation Rollback & State Repair", "Rejected tokens leave no trace anywhere.", ["KV truncation and block-table repair with O(1) cost", "sampler and RNG state rollback for exact determinism", "rollback-correctness verification via replay", "rollback cost measurement as a fraction of step time"]),
("cascade_tuning", "Draft Length & Depth Auto-Tuning", "Continuously finds the optimal speculation depth.", ["closed-loop controller from measured acceptance and latency", "per-request-class parameter tables", "stability analysis and safe parameter bounds", "measured improvement over fixed draft length"]),
("paged_attention_runtime", "Paged Attention Runtime Integration", "Wires the T02 paged decode kernel to the block-table manager.", ["block-table construction and validation per step", "page-fault-free guarantee via pre-reservation", "long-context split-KV coordination", "kernel-launch overhead elimination measurement"]),
("prefix_cache_runtime", "Prefix Cache Runtime & Radix Matching", "Turns repeated prompts into near-zero prefill cost.", ["radix-tree matching with longest-prefix lookup per request", "copy-on-write branching for shared prefixes", "eviction policy balancing hit rate and memory", "measured prefill-compute reduction on real traffic mixes"]),
("session_affinity", "Session Affinity & KV Reuse Routing", "Multi-turn conversations return to their own cache.", ["affinity routing with cache-locality scoring", "KV migration when affinity must break", "measured multi-turn latency improvement", "load-balance-versus-affinity tradeoff control"]),
("sampler_engine", "Sampling Engine & Decode Policies", "Every sampling mode, exact and fast.", ["temperature, top-k, top-p, min-p, typical and mirostat implementations", "repetition/presence/frequency penalties with correct accounting", "deterministic greedy path with tie-breaking rules", "distribution-fidelity statistical tests"]),
("constrained_decoding", "Grammar-Constrained Decoding Engine", "Structured output that is valid by construction, not by luck.", ["incremental parser (JSON/regex/CFG/schema) with token-mask computation", "precompiled automaton cache for hot schemas", "guarantee of syntactic validity with proof", "overhead measurement under 3% of decode time"]),
("logit_processor", "Logit Processing Pipeline", "Composable, ordered, auditable logit transformations.", ["processor chain with declared ordering semantics", "fused GPU-side processing avoiding host round-trips", "bias/ban/force-token mechanisms", "per-processor latency accounting"]),
("stop_conditions", "Stop Criteria & Output Boundary Detection", "Stops exactly where it should, including mid-token sequences.", ["multi-token stop-sequence detection across token boundaries", "max-token, deadline and budget stops with partial-result emission", "streaming-safe boundary handling for partial UTF-8", "exhaustive boundary-case tests"]),
("streaming_output", "Token Streaming & Backpressure", "First token fast, every token smooth, never a stall.", ["incremental detokenisation with correct multi-byte handling", "client-backpressure handling without blocking the engine", "chunking policy tuned for perceived latency", "inter-token-latency jitter measurement"]),
("batch_invariance", "Batch-Invariant Inference Mode", "Same answer whether you are alone or one of a thousand.", ["fixed-split reduction strategies independent of batch size", "batch-invariant attention and normalisation paths", "bit-exactness verification across batch sizes 1..1024", "measured cost of batch invariance"]),
("multi_gpu_runtime", "Multi-Device Execution Runtime", "Tensor, pipeline and expert parallel serving in one runtime.", ["parallel-plan execution with collective scheduling", "per-device stream coordination and event fencing", "rank-failure detection with request preservation", "scaling efficiency measurement across devices"]),
("model_loading", "Fast Model Loading & Warm Start", "From cold storage to serving in seconds, not minutes.", ["memory-mapped shard loading with lazy materialisation", "parallel load pipelines saturating storage bandwidth", "warmup kernel-compilation and cache priming", "cold-start-to-first-token measurement"]),
("weight_hotswap", "Zero-Downtime Weight Hot Swap", "Updates the model without dropping a single request.", ["dual-residency swap with atomic pointer flip", "in-flight-request version pinning for consistency", "rollback on quality-metric regression", "swap-duration and request-impact measurement"]),
("multi_model_serving", "Multi-Model Co-Residency & Time Slicing", "Many models on one machine without thrashing.", ["memory-aware model residency planner with LRU eviction", "time-slicing with SLO-aware quantum selection", "cross-model interference measurement and mitigation", "utilisation improvement measurement"]),
("adapter_serving", "Adapter & Fine-Tune Multiplexing", "Thousands of customised variants served from one base model.", ["batched multi-adapter execution with grouped kernels", "adapter cache with predictive loading", "per-adapter quality isolation verification", "throughput measurement at 1000 concurrent adapters"]),
("request_lifecycle", "Request Lifecycle & State Machine", "Every request's journey is explicit, observable and recoverable.", ["state machine with legal transitions and timeout at every state", "cancellation propagation with immediate resource release", "mid-flight migration between workers", "lifecycle-invariant verification under fault injection"]),
("cancellation", "Cancellation & Resource Reclamation", "A cancelled request stops costing money immediately.", ["cooperative cancellation checkpoints in every long operation", "in-flight tool-call cancellation with cleanup", "resource-reclaim verification (KV, memory, device time)", "cancellation-latency measurement"]),
("deadline_scheduling", "Deadline-Aware Execution", "Meets stated deadlines by degrading gracefully, never silently.", ["deadline propagation into kernel-level work decisions", "quality-degradation ladder triggered by deadline pressure", "partial-result emission with explicit completeness annotation", "deadline-attainment measurement under load"]),
("speculative_tools", "Speculative Tool Execution", "Runs likely tool calls before the model finishes asking (S6 source).", ["tool-call prediction from partial generation", "side-effect-free-only speculation with strict gating", "misprediction cost accounting and cancellation", "measured latency reduction on agent workloads"]),
("parallel_generation", "Parallel Multi-Branch Generation", "Explores several answers at once and keeps the best.", ["branch scheduling with shared prefix KV", "early pruning from verifier scores", "cost-capped exploration with budget accounting", "quality gain per extra dollar measurement"]),
("output_verification_loop", "Inline Output Verification Loop", "Checks its own answer before the user sees it.", ["verifier invocation policy with latency budget", "targeted regeneration of only the failing span", "convergence guarantees and iteration caps", "measured error-rate reduction versus single-pass output"]),
("engine_telemetry", "Engine Observability & Latency Attribution", "Explains exactly where every microsecond went.", ["per-phase latency decomposition (queue, prefill, decode, tools, verify)", "per-request cost and token accounting", "anomaly detection on latency distributions", "overhead under 1% with sampling controls"]),
("autoscaling", "Load Prediction & Autoscaling Controller", "Right-sizes capacity ahead of demand.", ["short-horizon load forecasting with uncertainty", "scale-up/down policy with cooldowns and cost constraints", "cold-capacity warmup coordination", "SLO attainment versus cost measurement"]),
("overload_shedding", "Overload Protection & Graceful Degradation", "Under extreme load it gets slower and cheaper, never broken.", ["priority-based load shedding with explicit user signalling", "quality-degradation ladder (fewer samples, smaller model, less search)", "shedding fairness across tenants", "behaviour verification at 10x rated load"]),
("engine_determinism", "Deterministic Serving Mode", "Reproducible outputs for audit, test and legal contexts.", ["end-to-end determinism: seeds, batching, reductions, tool order", "replay verification producing byte-identical outputs", "cost-of-determinism measurement", "documented exceptions with justification"]),
("kv_offload_runtime", "KV Offload & Recall Runtime", "Long conversations live in cheap memory until needed.", ["asynchronous KV demotion/promotion overlapped with decode", "recall-latency hiding via lookahead prediction", "stall-free guarantee under sustained offload", "cost reduction measurement at matched latency"]),
("quantised_serving", "Quantised Serving Paths & Quality Gates", "Fast, cheap precision without silent quality loss.", ["per-model precision plan execution with runtime verification", "canary quality checks comparing against a reference tier", "automatic fallback to higher precision on regression", "measured cost/quality frontier"]),
("engine_fault_tolerance", "Engine Fault Tolerance & Request Recovery", "A crashed worker loses zero requests.", ["request journaling with idempotent replay", "partial-output checkpointing for long generations", "device-failure detection and rapid rescheduling", "measured request-loss rate under injected faults"]),
("api_gateway_runtime", "Protocol Gateway & Request Normalisation", "One engine, many API dialects, one internal representation.", ["request validation, normalisation and default resolution", "streaming-protocol adapters (SSE, websocket, gRPC)", "backward-compatibility handling across API versions", "gateway overhead measurement under 200 microseconds"]),
("engine_bench_serving", "Serving Benchmark & Load Generator", "The canonical performance measurements for the engine.", ["realistic traffic generation from production-shaped distributions", "TTFT, ITL, throughput and cost metrics with variance bands", "comparison harness versus published Opus-class latencies", "regression gate with statistical significance"]),
("cascade_speed_proof", "S1 Speedup Proof & Attribution", "Proves the 4.0x speculative-cascade component of the 100x law.", ["acceptance-rate to speedup derivation with measured inputs", "interaction analysis with batching and sparsity factors", "confidence intervals over traffic mixes", "attribution report feeding the 100x proof"]),
("engine_config_tuning", "Engine Configuration Auto-Tuning", "Finds the best of thousands of engine settings automatically.", ["parameter space definition with legality constraints", "SLO-constrained optimisation via bandit search on live traffic", "safe-exploration guardrails and instant rollback", "measured improvement over hand-tuned defaults"]),
("tokenizer_runtime", "Runtime Tokenisation & Detokenisation", "Tokenisation is never the bottleneck, and never wrong.", ["high-throughput batched tokenisation with SIMD scanning", "incremental detokenisation with correct grapheme handling", "cross-language consistency verification", "throughput measurement in millions of tokens per second"]),
("prompt_compilation", "Prompt Compilation & Static Prefill", "Compiles fixed prompt scaffolding once, forever.", ["prompt template compilation into precomputed KV artifacts", "slot-filling with position-correct KV splicing", "artifact cache with version invalidation", "measured prefill saving for templated workloads"]),
("engine_security", "Serving-Path Security Hardening", "The engine is a hostile-input processor: it must be bulletproof.", ["input validation, size limits and resource-exhaustion defences", "tenant isolation verification at every shared structure", "timing-side-channel analysis on shared caches", "adversarial-input fuzzing of the whole request path"]),
("cost_accounting_runtime", "Per-Request Cost Accounting", "Every request knows what it cost, to the microdollar.", ["token, FLOP, device-time and energy accounting per request", "attribution across cascade stages and tool calls", "billing-grade accuracy verification", "cost-report generation for the platform tier"]),
("engine_spec_doc", "Inference Engine Specification & Runbook", "The authoritative description and operations guide.", ["specification of scheduling, guarantees, SLOs and degradation modes", "auto-generated configuration and capacity tables", "incident runbook covering every failure mode", "spec-versus-implementation drift detection"]),
]
