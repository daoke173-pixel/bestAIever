PARTS = [
("abi_types", "Core ABI Scalar & Tensor Type System", "Define every scalar, vector, tensor, ragged and symbolic dtype used across all 1000 parts, with exact bit layouts.", ["fp8-e4m3/e5m2, bf16, fp16, tf32, int4/int8 packed, mx-block formats with shared exponent", "shape algebra with symbolic dims, broadcasting lattice, and compile-time rank checking", "canonical byte layout + endianness normalisation and zero-copy views", "saturating/wrapping cast matrix with round-to-nearest-even and stochastic rounding"]),
("abi_errors", "OmegaError Taxonomy & Cause Chains", "Implement the 9-range error model with immutable cause chains, remedies and budget accounting.", ["OmegaCode enum with 240 documented codes and stable numeric values", "cause-chain builder that is allocation-bounded and cycle-safe", "retryability classifier plus exponential-decorrelated-jitter policy objects", "redaction pass so errors never leak secrets or user content"]),
("abi_result", "Result / Partial / Budget Monad", "Provide the universal return container carrying value, partiality, budget spend and provenance.", ["Result<T> with Ok/Partial/Err and monadic map/and_then/recover", "budget ledger arithmetic with saturating subtraction and overspend detection", "partial-result merge semantics for fan-out calls", "zero-cost conversion to/from the wire envelope"]),
("cbor_canonical", "Deterministic CBOR Codec", "RFC 8949 deterministic CBOR encode/decode used for every payload on the Ω-Bus.", ["bytewise-sorted map keys, shortest-form integers, no indefinite lengths", "streaming decoder with depth/size limits and no unbounded allocation", "schema-guided fast paths for the 40 hottest message types", "differential fuzz harness against a naive reference encoder"]),
("blake3_hash", "BLAKE3 & Merkle Integrity Core", "Content hashing, keyed MAC and Merkle trees for envelope integrity and artifact identity.", ["portable BLAKE3 with SIMD-friendly block loop and incremental XOF", "Merkle tree over chunked artifacts with inclusion proof generation/verification", "keyed derivation for per-part integrity tags", "constant-time tag comparison"]),
("chacha_seeds", "split_seed Deterministic RNG", "The one and only randomness source: ChaCha20-based hierarchical seed splitting.", ["split_seed(root, part_id, call_index) with domain separation strings", "counter-based streams enabling replay and parallel reproducibility", "distributions: uniform, normal (ziggurat), categorical (alias table), gumbel", "cross-platform bit-exactness test vectors"]),
("omega_bus_core", "Ω-Bus Capability Registry", "In-process capability registry with versioned lookup, fallbacks and link-time validation.", ["acquire/publish with semver major pinning and ambiguity detection", "declared-fallback resolution when a capability is absent", "topological link validation producing a human-readable unresolved report", "handle lifetime, refcounting and safe teardown ordering"]),
("omega_bus_ipc", "Cross-Process Ω-Bus Transport", "Shared-memory + unix-socket transport so parts can live in separate processes/machines.", ["ring-buffer SPSC/MPMC queues with cacheline padding and no false sharing", "zero-copy payload handoff via memfd/shm segments with lifetime tokens", "backpressure, credit-based flow control and head-of-line-blocking avoidance", "crash detection with epoch fencing so a dead peer cannot corrupt shared state"]),
("envelope_codec", "OmegaEnvelope Encode/Decode", "The wire envelope: trace context, deadline, budget, provenance and integrity tag.", ["fixed-layout header for branch-free parsing plus variable CBOR body", "deadline arithmetic on a monotonic clock with skew guards", "provenance append with size cap and eviction policy", "integrity verify-then-use ordering with tamper telemetry"]),
("trace_context", "Distributed Trace & Span Model", "128-bit trace ids, causal span trees and sampling that survives 1000-way fan-out.", ["W3C-compatible trace context plus Ω extensions for budget propagation", "causal parent tracking across async boundaries and thread hops", "adaptive tail-based sampling that always keeps errors and slow spans", "span buffer with bounded memory and lock-free append"]),
("clock_time", "Monotonic Clock & Deadline Arithmetic", "Time primitives: monotonic ns, TSC calibration, deadlines, timers and virtual clocks.", ["TSC-to-ns calibration with drift correction and invariant-TSC detection", "hierarchical timer wheel for millions of pending deadlines", "virtual clock injection for deterministic replay of timeout logic", "deadline propagation with reserve-and-release semantics"]),
("alloc_arena", "Arena & Slab Allocators", "Deterministic allocation: bump arenas, size-class slabs and NUMA-aware pools.", ["bump arena with scope guards and O(1) reset", "slab allocator with per-thread magazines and cross-thread free lists", "NUMA-local pools with first-touch policy and hugepage backing", "allocation accounting with high-water marks and leak assertions"]),
("mem_layout", "Cache-Aware Data Layout Toolkit", "SoA/AoS transforms, tiling and alignment utilities that all hot loops build on.", ["struct-of-arrays generator with padding computation", "tile-shape solver for a given cache hierarchy description", "software prefetch scheduling helpers with distance tuning", "layout equivalence checker used by the compiler tier"]),
("atomics_sync", "Lock-Free Synchronisation Primitives", "The concurrency toolbox: epochs, seqlocks, hazard pointers, futex parking.", ["epoch-based reclamation with bounded garbage", "seqlock and versioned snapshot reads for hot config", "MCS/ticket locks with adaptive spin-then-park", "ABA-safe treiber stack and Michael-Scott queue"]),
("task_runtime", "Deterministic Task Scheduler", "Work-stealing runtime with a deterministic mode for reproducible parallel execution.", ["work-stealing deques with random-then-round-robin victim selection", "deterministic replay mode with a recorded schedule log", "priority classes, deadline-aware admission and starvation freedom proof", "structured concurrency scopes with cancellation propagation"]),
("dataflow_dag", "Task DAG Representation & Scheduling", "The DAG type that drives 1000-way parallel horizon execution (speed source S6).", ["immutable DAG builder with cycle detection and critical-path computation", "list scheduling with speculative eager start and rollback", "resource-constrained scheduling under budget and device limits", "DAG diffing for incremental re-execution"]),
("fixed_point", "Certified Fixed-Point & Interval Arithmetic", "Exact and interval numerics used by verification and certified inference paths.", ["Q-format fixed point with proven bounds and saturation semantics", "interval and affine arithmetic with correct outward rounding", "error-bound propagation for composed operations", "cross-check against arbitrary-precision reference"]),
("bigint_modmath", "Big Integer & Modular Arithmetic", "Multi-precision integers, modular reduction and NTT used by crypto and exact reasoning.", ["schoolbook/Karatsuba/Toom-Cook thresholds with tuned crossovers", "Montgomery and Barrett reduction, constant-time where required", "NTT-based multiplication for very large operands", "GCD, modular inverse, primality (Miller-Rabin + BPSW)"]),
("bitset_rank", "Succinct Bitsets, Rank/Select & Bloom Filters", "Space-efficient set structures used by routers, caches and retrieval indexes.", ["rank/select with interleaved superblocks and O(1) queries", "Roaring-style hybrid containers with adaptive representation", "blocked Bloom and cuckoo filters with measured FPR curves", "SIMD-friendly population count and set operations"]),
("hash_maps", "High-Performance Hash Containers", "Open-addressing maps/sets with SIMD probing that back every registry and cache.", ["group-probe (SSE/NEON-style) metadata scanning with tombstone control", "seeded hashing with HashDoS resistance", "incremental rehashing to avoid latency spikes", "concurrent sharded variant with per-shard seqlocks"]),
("string_interning", "String Interning & Symbol Tables", "Global-free symbol interning with stable ids for module, capability and token names.", ["lock-free intern table with generation-stable u32 symbols", "prefix-compressed storage and reverse lookup", "capability-URI parser producing pre-hashed symbols", "collision and exhaustion policies"]),
("arena_graph", "Generic Graph Store & Algorithms", "CSR graph storage plus the traversal kernels reused by compiler, memory and reasoning tiers.", ["CSR/CSC build with parallel counting sort", "BFS/DFS/SCC/topo-sort/dominator computation", "shortest paths (delta-stepping) and min-cut helpers", "graph mutation with copy-on-write snapshots"]),
("serialization_schema", "Schema Registry & Evolution Rules", "Machine-readable schemas for every capability, with compatibility checking.", ["schema IDL, canonical JSON projection and hash-based schema ids", "forward/backward compatibility checker with precise diagnostics", "code-generation-free runtime validation with compiled validators", "registry snapshot embedding so a part validates offline"]),
("config_system", "Layered Configuration & Feature Flags", "Deterministic configuration with provenance, validation and hot reload.", ["layer merge (defaults < file < env < request) with per-key provenance", "typed schema validation and range/unit checks", "atomic snapshot swap readable from hot loops without locks", "flag lifecycle: default, rollout percentage, kill switch"]),
("logging_events", "Structured Event Emission Core", "The emit_event pipeline: bounded, lock-free, sampling-aware, never blocking.", ["per-thread ring buffers with a single drain thread", "level+code based filtering evaluated before argument formatting", "overflow accounting so drops are visible rather than silent", "redaction hooks for user content and secrets"]),
("metrics_core", "Counters, Gauges & HDR Histograms", "Numeric telemetry primitives with exact percentiles at nanosecond resolution.", ["striped atomic counters with per-CPU shards", "HDR histogram with configurable significant digits and merge", "exponentially decaying reservoirs for rate estimation", "export snapshot that is allocation-free on the hot path"]),
("selftest_harness", "In-File Self-Test Framework", "The tiny test framework every part embeds so tests need no shared fixtures.", ["assertion set with structured failure diffs", "property-based generator combinators and shrinking", "deterministic seed control and failure reproduction strings", "SelfTestReport aggregation and machine-readable output"]),
("property_gen", "Property Generators & Shrinkers", "Reusable generators for tensors, graphs, strings, schedules and adversarial inputs.", ["combinator library with size control and coverage feedback", "shrinking strategies preserving invariants", "domain generators: shapes, dtypes, capability URIs, envelopes", "corpus persistence format for regression pinning"]),
("fuzz_engine", "Coverage-Guided In-Process Fuzzer", "The fuzzing engine parts use to satisfy the adversarial-test contract clause.", ["mutation stack: bitflip, splice, dictionary, structure-aware", "in-process coverage feedback via instrumentation shims", "crash/hang triage with minimisation", "time-boxed budget so it fits the 60 s test limit"]),
("bench_harness", "Microbenchmark Harness & Latency Gate", "Statistically sound in-file benchmarking that asserts declared p99 budgets.", ["warmup detection, outlier-robust estimation, CI computation", "CPU pinning/frequency-noise detection and reporting", "regression gate comparing to a checked-in baseline record", "BenchReport schema shared by all parts"]),
("determinism_replay", "Record & Replay Engine", "Records every nondeterminism source so any run can be replayed bit-exactly.", ["capture of seeds, clocks, schedules, tool results and device order", "compact trace format with content-addressed dedup", "divergence detector pinpointing the first differing event", "replay-under-modification for counterfactual debugging"]),
("checksum_verify", "Cross-Platform Numeric Equivalence Checker", "Proves that a computation is bit-identical across platforms and batch shapes.", ["tolerance-free bit comparison plus classified tolerance modes", "batch-invariance and thread-count-invariance test drivers", "reduction-order pinning helpers", "report that localises the first divergent op"]),
("capability_gate", "Capability Acquisition Policy & Fallbacks", "Policy layer over the bus: allowlists, quotas, degradation ladders.", ["per-caller capability allowlists with deny-by-default option", "quota and rate limiting per capability and per trace", "declared degradation ladder execution with quality annotation", "audit record for every acquisition decision"]),
("version_semver", "Semantic Version & Compatibility Engine", "Version parsing, range solving and the link-time compatibility matrix.", ["semver parse/compare with prerelease ordering", "range solver for the 1000-part dependency set", "breaking-change detector across schema snapshots", "diagnostic output naming the exact conflicting parts"]),
("link_validator", "Static Link Validator for 1000 Parts", "Validates the whole assembly: every requires resolved, no cycles, no duplicate provides.", ["manifest ingestion and normalisation from all 1000 files", "duplicate/ambiguous provider detection with resolution hints", "cycle detection with minimal counterexample path", "assembly report artifact consumed by the platform tier"]),
("manifest_parser", "PART_MANIFEST Parser & Validator", "Extracts and validates manifests from source files in any of the three languages.", ["language-agnostic extraction with tolerant parsing", "field-level validation, URI syntax, budget sanity checks", "digest check for the frozen contract header", "batch report over 1000 files in under a second"]),
("abi_stability", "ABI Stability & Frozen-Symbol Guard", "Detects any change that would break already-written parts.", ["symbol inventory snapshot with signature hashes", "additive-only enforcement for frozen namespaces", "deprecation ladder with compile-time warnings", "waiver file with expiry dates and owners"]),
("compat_shims", "Cross-Language Interop Shims", "Uniform FFI surface so Python, Rust and TypeScript parts share the same ABI.", ["C-ABI boundary definition with ownership rules", "buffer protocol bridging without copies", "error mapping across language boundaries preserving cause chains", "async bridging with cancellation propagation"]),
("numeric_limits", "Numeric Policy, Rounding & Overflow Rules", "One policy for how every kernel handles rounding, denormals, NaN and overflow.", ["policy objects selecting round mode, denormal handling, FMA use", "NaN/Inf propagation contract and quiet/signalling rules", "overflow detection strategies per dtype and per op class", "conformance suite that all kernels must pass"]),
("unit_dimensions", "Physical Units & Dimensional Analysis", "Compile-time dimension checking used by science, engineering and cost accounting.", ["dimension vectors with compile-time (or checked) arithmetic", "unit conversion with exact rationals to avoid drift", "quantity type that refuses invalid operations", "cost/energy/token units treated as first-class dimensions"]),
("budget_ledger", "Token / FLOP / Dollar Budget Ledger", "The accounting system that makes 'faster and cheaper' measurable and enforced.", ["hierarchical budget reservation with parent-child rollup", "atomic spend with overspend rejection and partial-result signalling", "conversion table between tokens, FLOPs, joules and micro-dollars", "postmortem attribution report per part and per span"]),
("rate_limiter", "Deterministic Rate Limiting & Admission", "Token buckets, GCRA and concurrency limiters with deterministic behaviour.", ["GCRA with monotonic-clock precision and no drift", "hierarchical buckets for nested resource classes", "concurrency limiter with latency-target auto-tuning", "fair queueing across 1000 concurrent callers"]),
("circuit_breaker", "Failure Isolation & Circuit Breaking", "Prevents one failing part from degrading the 1000-part assembly.", ["rolling-window failure statistics with EWMA and quantiles", "half-open probing with jittered recovery", "bulkhead isolation pools per capability", "outlier ejection driven by comparative latency"]),
("retry_idempotency", "Retry, Idempotency & Exactly-Once Effects", "Safe retries for tool calls and side-effecting actions.", ["idempotency keys with content-derived derivation", "effect log with commit/abort and fencing tokens", "at-most-once wrapper for irreversible actions", "duplicate-suppression window with bounded memory"]),
("secure_zeroize", "Secret Handling & Memory Hygiene", "Secrets, key material and user content lifetimes.", ["zeroize-on-drop buffers resistant to optimisation removal", "mlock/no-swap regions and core-dump exclusion", "constant-time comparison and branch-free selection helpers", "taint tracking so secrets cannot enter logs or prompts"]),
("sandbox_policy", "Process Sandbox & Syscall Policy Descriptors", "Declarative sandbox profiles that agent and tool parts execute under.", ["capability-to-syscall mapping with deny-by-default profiles", "filesystem and network allowlist descriptors", "resource rlimits, cgroup descriptors and OOM policy", "profile verification tests that assert denied operations fail"]),
("fs_atomic", "Atomic Filesystem & Content-Addressed Store", "Crash-safe artifact storage used for caches, traces and model shards.", ["write-temp-fsync-rename with directory fsync ordering", "content-addressed layout with hardlink dedup and GC", "lock files with stale-owner detection", "corruption detection and self-healing repair pass"]),
("compression", "Streaming Compression & Delta Encoding", "Compression primitives for KV caches, traces and artifacts.", ["LZ77-family streaming codec with dictionary support", "delta encoding for near-duplicate tensors and texts", "entropy coder (rANS) with speed/ratio presets", "throughput/ratio benchmark matrix used for policy selection"]),
("bootstrap_init", "Assembly Bootstrap & Startup Ordering", "Brings the 1000 parts up in a correct, fast, observable order.", ["dependency-ordered register() invocation with parallel waves", "readiness/liveness state machine per part", "lazy-initialisation policy for cold capabilities", "startup latency budget with per-part attribution"]),
("shutdown_drain", "Graceful Drain & Crash-Consistency", "Deterministic teardown that never loses an in-flight envelope.", ["drain phases: stop-admit, finish-inflight, flush, release", "deadline-bounded drain with forced-abort accounting", "crash-consistency invariants and recovery replay", "shutdown correctness tests under injected faults"]),
]
